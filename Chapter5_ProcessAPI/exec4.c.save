#include <stdlib.h>
#include <unistd.h>
#include <stdio.h>
#include <string.h>

int main (int argc, char *argv[]) {
	int rc = fork();
	if (rc < 0) {
		printf(stderr, "fork failed\n");
		exit(1);
	} else if (rc == 0) {
		char *lsargs[2];
		lsargs[0] = strdup("/bin/ls");
		lsargs[1] = NULL;
		execvp(lsargs[0], lsargs);
	} else {


